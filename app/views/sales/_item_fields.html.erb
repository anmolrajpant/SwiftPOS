<fieldset class="form-group">  
  <%= f.label :product, "Product" %>  
  <%= f.collection_select :product_id, Product.all, :id, :name, id: "dropdown" %>  
  <%= f.label :quantity, "Quantity", id:"qua"%>  
  <%= f.text_field :quantity, class: 'padli' %>  
  <%= f.hidden_field :_destroy %>   
  <%= link_to "remove", '#', class: "remove_fields" %>  
</fieldset>

<script>
        $(document).ready(function() { $("#dropdown").select2(); });
</script>